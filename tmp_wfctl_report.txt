## 存量制品检测摘要 — CSLT-02 RAG语义检索

| 制品类型 | 状态 | 路径/说明 |
|:---|:---|:---|
| 意图文档 | 不存在 | `docs/功能设计/02-智能应急咨询/CSLT-02-RAG语义检索/CSLT-02-RAG语义检索-意图文档.md` — 分组目录 02-智能应急咨询 不存在 |
| 设计文档 | 不存在 | 同上 |
| 落地规范 | 不存在 | 同上 |
| 契约文件 | 不存在 | `contracts/CSLT-02/` 目录不存在；`docs/contracts/_index.json` 无 CSLT-02 条目 |
| 源代码 | 无有效实现 | `packages/py-rag/` 存在但仅含空骨架（`__init__.py` 仅有 `print("Hello from py-rag!")` stub），无实际业务逻辑。其余源码文件中未检出 CSLT-02 或 RAG 语义检索相关实现 |
| 同步问题 | 无未解决项 | `docs/功能设计/_sync-issues.md` 无 CSLT-02 相关条目 |

## 路由推荐

**推荐场景**：`full_design`

**推理**：模块 CSLT-02（RAG语义检索）在设计拆解表中登记为"未开始"，所属分组 `02-智能应急咨询` 在 `docs/功能设计/` 下完全不存在。经过全量检测：
- 无任何设计文档（意图文档、设计文档、落地规范） — 完整缺失
- 无任何契约文件 — `contracts/` 和 `docs/contracts/` 均无 CSLT-02 条目
- 无实际源代码实现 — `packages/py-rag/` 仅含 GitHub 初始化时的空占位文件，`__init__.py` 仅含 `print` stub，不作为有效代码实现认定
- 功能模块全拆解表确认设计状态为"未开始"，与扫描结果一致

属于"全新模块、无任何存量资产"的标准场景，推荐从头开始完整设计流程。

**推荐起点**：从制品检测开始 -> 意图澄清

**推荐跳过**：无

**替代路径**（用户仍可选择）：
- `code_only`：认为 packages/py-rag/ 空骨架属于有效代码，走逆向推导路径。风险：该目录无实际逻辑可逆向，将导致逆向推导阶段产出空结果或需要返工

**关键边界说明**：
- `02-智能应急咨询` 分组目录在 `docs/功能设计/` 中不存在，意味着 CSLT 系列全部 8 个模块均未进入设计流程，本次设计需要同时创建分组目录和模块子目录
- 代码检测置信度为 low — `packages/py-rag/` 的存在表明项目预留了 RAG 模块位置，但内容为空，不影响 full_design 判定
