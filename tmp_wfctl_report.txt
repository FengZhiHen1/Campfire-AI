## 存量制品检测摘要 — CSLT-01 危机分级判定

| 制品类型 | 状态 | 路径/说明 |
|:---|:---|:---|
| 意图文档 | ❌ 不存在 | — |
| 设计文档 | ❌ 不存在 | — |
| 落地规范 | ❌ 不存在 | — |
| 契约文件 | ❌ 不存在 | — |
| 源代码 | ❌ 未检测到 | — |
| 同步问题 | ✅ 无未解决项 | `docs/功能设计/_sync-issues.md` |
| 原始材料 | ⚠️ 存在但非正式设计文档 | `docs/功能设计/原始材料/细节设计/评定危机等级机制.md` |

## 路由推荐

**推荐场景**：`full_design`

**推理**：CSLT-01（危机分级判定）为全新模块——模块目录 `docs/功能设计/02-智能应急咨询/` 尚不存在，功能模块全拆解表设计状态标注为「未开始」。经全面扫描确认：无任何正式设计文档（意图/设计/规范均缺失）、无契约文件、无可检测代码。存在原始材料（`评定危机等级机制.md`）详细描述了三层递进判定架构，可作为设计阶段的输入参考，但不属于正式设计制品。

**推荐起点**：存量检测阶段后 → 意图澄清（全流程从头开始）

**跳过的步骤**：无

**替代路径**（用户仍可选择）：
- `code_only`：⚠️ 不适用——本模块尚无任何代码实现
- `design_docs_only_intent_draft`：⚠️ 不适用——本模块尚无任何意图文档

**预计耗时（基于模块类型 🔴 核心）**：约 15 分钟

## 关键上下文

- CSLT-01 属于「02-智能应急咨询」分组下的首个模块（编号最小）
- 依赖关系：依赖 PROF-02（档案标签数据），与 SEC-02、KNOW-05 共享高风险关键词库
- 已有原始设计材料 `评定危机等级机制.md` 可作为意图文档和设计文档的参考输入
- 下游模块 CSLT-02~CSLT-08 均依赖 CSLT-01 的设计成果（危机分级结果注入 Prompt）
- 模块类型「🔴 核心」，对应检查清单第 3 条（复杂业务规则）和第 5 条（产品核心差异价值）
