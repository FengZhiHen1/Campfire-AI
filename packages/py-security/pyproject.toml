[project]
name = "py-security"
version = "0.1.0"
description = "Campfire-AI shared package: py-security — PII detection, security utilities"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
