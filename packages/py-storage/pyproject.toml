[project]
name = "py-storage"
version = "0.1.0"
description = "Campfire-AI shared package: py-storage"
requires-python = ">=3.12"
dependencies = [
    "py-config",
    "python-magic>=0.4.27",
]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
