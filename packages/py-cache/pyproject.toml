[project]
name = "py-cache"
version = "0.1.0"
description = "Campfire-AI shared package: py-cache"
requires-python = ">=3.12"
dependencies = [
    "py-config",
    "redis>=5.0",
]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
