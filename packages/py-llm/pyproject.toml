[project]
name = "py-llm"
version = "0.1.0"
description = "Campfire-AI shared package: py-llm"
requires-python = ">=3.12"
dependencies = ["openai>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["py_llm"]
